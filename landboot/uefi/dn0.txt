	

	UEFI - Unified Extensible Firmware Interface

	See:
	https://www.uefi.org/
	https://en.wikipedia.org/wiki/Unified_Extensible_Firmware_Interface
	...


	The root dir will need the folders EFI/ and EFI/BOOT/ 
	inside a FAT32 boot partition.

	We are using a FAT16 boot partition.
	We will use this if it is possible, if not we
	will make a smoothly transition to FAT32.
